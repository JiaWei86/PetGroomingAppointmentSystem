@model PetGroomingAppointmentSystem.Areas.Staff.ViewModels.CalendarAppointmentModel
@{
    ViewData["Title"] = "Appointment";
    ViewData["ActivePage"] = "Appointment";
}
@section Styles {
    <link rel="stylesheet" href="~/admin/css/responsive.css" />
    <style>
        .appointment-wrapper {
            min-height: calc(100vh - 70px);
            margin-top: 70px;
            background: linear-gradient(135deg, #f0fdf4 0%, #f9fafb 100%);
            padding: 40px 20px;
        }

        .appointment-container {
            max-width: 1400px;
            width: 100%;
            margin: 0 auto;
            background: white;
            border-radius: 16px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .appointment-container:hover {
            transform: translateY(-4px);
            box-shadow: 0 16px 56px rgba(0, 0, 0, 0.15);
        }

        .appointment-header {
            background: linear-gradient(135deg, #d97706 0%, #b45309 100%);
            color: white;
            padding: 40px;
            position: relative;
            overflow: hidden;
        }

        .appointment-header::before {
            content: '';
            position: absolute;
            top: 0;
            right: 0;
            width: 300px;
            height: 300px;
            background: radial-gradient(circle, rgba(255,255,255,0.1) 0%, transparent 70%);
            border-radius: 50%;
        }

        .appointment-header-content {
            position: relative;
            z-index: 1;
        }

        .appointment-header-title {
            font-size: 28px;
            font-weight: 800;
            margin: 0 0 8px 0;
            letter-spacing: -0.5px;
        }

        .appointment-header-subtitle {
            font-size: 15px;
            opacity: 0.95;
            font-weight: 500;
        }

        .appointment-body {
            padding: 40px;
        }

        .calendar-section {
            display: grid;
            grid-template-columns: 1fr 360px;
            gap: 28px;
            align-items: stretch;
        }

        /* ========== CALENDAR STYLES ========== */
        .calendar-container {
            background: white;
            border-radius: 12px;
            padding: 20px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
        }

        .calendar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            flex-shrink: 0;
        }

        .calendar-header h2 {
            margin: 0;
            font-size: 20px;
            color: #374151;
        }

        .calendar-nav {
            display: flex;
            gap: 12px;
            align-items: center;
        }

        .month-year {
            font-size: 16px;
            font-weight: 600;
            color: #1f2937;
            min-width: 180px;
            text-align: center;
        }

        .btn-nav {
            background: linear-gradient(135deg, #d97706 0%, #b45309 100%);
            color: white;
            border: none;
            border-radius: 6px;
            padding: 8px 12px;
            cursor: pointer;
            font-size: 16px;
            transition: all 0.3s ease;
        }

        .btn-nav:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(217, 119, 6, 0.3);
        }

        .calendar-grid-wrapper {
            background: #f9fafb;
            border-radius: 8px;
            padding: 16px;
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .calendar-weekdays {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 8px;
            margin-bottom: 12px;
            flex-shrink: 0;
        }

        .calendar-day-header {
            text-align: center;
            font-weight: 700;
            font-size: 12px;
            color: #d97706;
            padding: 8px 4px;
            letter-spacing: 0.5px;
        }

        #calendar {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 8px;
            flex: 1;
        }

        .calendar-day {
            aspect-ratio: 1;
            border: 2px solid #e5e7eb;
            border-radius: 8px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            background: white;
            position: relative;
            transition: all 0.3s ease;
            min-height: 60px;
        }

        .calendar-day:hover {
            border-color: #d97706;
            box-shadow: 0 4px 12px rgba(217, 119, 6, 0.1);
            transform: translateY(-2px);
        }

        .calendar-day.empty {
            border: none;
            cursor: default;
            background: transparent;
        }

        .calendar-day.empty:hover {
            border: none;
            box-shadow: none;
            transform: none;
        }

        .calendar-day.today {
            border-color: #d97706;
            background: linear-gradient(135deg, #fef3c7 0%, #fffbeb 100%);
        }

        .calendar-day.today .day-number {
            color: #d97706;
            font-weight: 700;
        }

        .calendar-day.selected {
            border-color: #10b981;
            background: linear-gradient(135deg, #d1fae5 0%, #ecfdf5 100%);
        }

        .calendar-day.selected .day-number {
            color: #10b981;
            font-weight: 700;
        }

        .calendar-day.has-appointments {
            background: linear-gradient(135deg, #dbeafe 0%, #f0f9ff 100%);
            border-color: #0ea5e9;
        }

        .calendar-day.all-completed {
            background: linear-gradient(135deg, #dcfce7 0%, #f0fdf4 100%);
            border-color: #10b981;
        }

        .calendar-day.all-completed .day-number {
            color: #166534;
            font-weight: 700;
        }

        .day-number {
            font-size: 16px;
            font-weight: 600;
            color: #374151;
            transition: color 0.3s ease;
        }

        .appointment-dot {
            width: 6px;
            height: 6px;
            background: #0ea5e9;
            border-radius: 50%;
            position: absolute;
            bottom: 6px;
            display: block;
        }

        .calendar-day.all-completed .appointment-dot {
            background: #10b981;
        }

        /* ========== SIDEBAR STYLES ========== */
        .appointments-sidebar {
            background: white;
            border-radius: 12px;
            padding: 20px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            height: 100%;
            min-height: 600px;
        }

        .sidebar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            padding-bottom: 16px;
            border-bottom: 2px solid #f3f4f6;
            flex-shrink: 0;
        }

        .sidebar-title {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 16px;
            font-weight: 700;
            color: #1f2937;
        }

        .sidebar-title i {
            font-size: 20px;
            color: #d97706;
        }

        .selected-date-badge {
            background: linear-gradient(135deg, #d97706 0%, #b45309 100%);
            color: white;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            white-space: nowrap;
        }

        #appointmentsList {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 12px;
            overflow: auto;
            padding-right: 0;
            max-height:650px;
        }

        .appointment-item {
            padding: 14px;
            background: #f9fafb;
            border-left: 4px solid #0ea5e9;
            border-radius: 6px;
            transition: all 0.3s ease;
            display: flex;
            flex-direction: column;
            gap: 8px;
            flex-shrink: 0;
        }

        .appointment-item:hover {
            background: #eff6ff;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
            transform: translateX(2px);
        }

        .appointment-item.completed {
            opacity: 0.7;
            background: #f0fdf4;
            border-left-color: #10b981;
        }

        .appointment-time {
            font-weight: 700;
            color: #1f2937;
            font-size: 14px;
            margin: 0;
        }

        .appointment-details {
            display: flex;
            flex-direction: column;
            gap: 3px;
            font-size: 13px;
            margin: 0;
        }

        .appointment-pet {
            color: #374151;
            font-weight: 600;
            margin: 0;
        }

        .appointment-service {
            color: #6b7280;
            margin: 0;
            font-size: 12px;
        }

        .appointment-groomer {
            color: #9ca3af;
            font-style: italic;
            margin: 0;
            font-size: 12px;
        }

        .appointment-actions {
            display: flex;
            gap: 8px;
            margin-top: 2px;
        }

        .btn-status-update {
            padding: 6px 12px;
            font-size: 0.8rem;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            background-color: #10b981;
            color: white;
            transition: all 0.3s ease;
            font-weight: 600;
            white-space: nowrap;
            flex-shrink: 0;
        }

        .btn-status-update:hover {
            background-color: #059669;
            transform: translateY(-1px);
        }

        .btn-status-update:disabled {
            background-color: #d1d5db;
            cursor: not-allowed;
        }

        .appointment-status {
            display: inline-block;
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 0.75rem;
            font-weight: 600;
            white-space: nowrap;
        }

        .appointment-status.confirmed {
            background-color: #dbeafe;
            color: #1e40af;
        }

        .appointment-status.completed {
            background-color: #dcfce7;
            color: #166534;
        }

        .appointment-status-wrapper {
            display: flex;
            gap: 8px;
            align-items: center;
            flex-wrap: wrap;
        }

        .no-appointments {
            text-align: center;
            color: #9ca3af;
            padding: 60px 12px;
            font-style: italic;
            margin: auto;
        }F

        .loading-spinner {
            display: inline-block;
            width: 14px;
            height: 14px;
            border: 2px solid #f3f4f6;
            border-top: 2px solid #10b981;
            border-radius: 50%;
            animation: spin 0.8s linear infinite;
        }

        @@keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* ========== RESPONSIVE ========== */
        @@media (max-width: 1024px) {
            .calendar-section {
                grid-template-columns: 1fr;
            }

            .appointments-sidebar {
                position: static;
            }
        }

        @@media (max-width: 768px) {
            .calendar-day {
                min-height: 50px;
                font-size: 12px;
            }

            .day-number {
                font-size: 14px;
            }

            .appointment-body {
                padding: 20px;
            }
        }
    </style>
}

<div class="appointment-wrapper">
    <div class="appointment-container">
        <div class="appointment-header">
            <div class="appointment-header-content">
                <h1 class="appointment-header-title">📅 Appointment Schedule</h1>
            </div>
        </div>

        <div class="appointment-body">
            <!-- Calendar Section -->
            <div class="calendar-section">
                @Html.AntiForgeryToken()
                <div class="calendar-container">
                    <div class="calendar-header">
                        <h2>Appointment Calendar</h2>
                        <div class="calendar-nav">
                            <button id="prevMonth" class="btn-nav">&laquo;</button>
                            <span id="monthYear" class="month-year"></span>
                            <button id="nextMonth" class="btn-nav">&raquo;</button>
                        </div>
                    </div>

                    <div class="calendar-grid-wrapper">
                        <div class="calendar-weekdays">
                            <div class="calendar-day-header">SUN</div>
                            <div class="calendar-day-header">MON</div>
                            <div class="calendar-day-header">TUE</div>
                            <div class="calendar-day-header">WED</div>
                            <div class="calendar-day-header">THU</div>
                            <div class="calendar-day-header">FRI</div>
                            <div class="calendar-day-header">SAT</div>
                        </div>

                        <div id="calendar"></div>
                    </div>
                </div>

                <div class="appointments-sidebar">
                    <div class="sidebar-header">
                        <div class="sidebar-title">
                            <i class="material-icons">event</i>
                            <span>Appointments</span>
                        </div>
                        <span id="selectedDate" class="selected-date-badge">Today</span>
                    </div>

                    <div id="appointmentsList">
                        <p class="no-appointments">No appointments scheduled</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    // ========================================
    // STAFF CALENDAR DATA
    // ========================================

    // Dynamically inject appointment data from the ViewModel
    // Status is now fetched directly from the database via the controller
    const appointmentDataJson = '@Html.Raw(Json.Serialize(Model?.AppointmentsForCalendar ?? new List<PetGroomingAppointmentSystem.Models.ViewModels.CalendarAppointmentModel>()))';
    const appointmentData = appointmentDataJson ? JSON.parse(appointmentDataJson) : [];
    
    // DEBUG: Log the data to inspect structure
    console.log('Appointment Data:', appointmentData);
    if (appointmentData.length > 0) {
        console.log('First appointment:', appointmentData[0]);
        console.log('Status value:', appointmentData[0].status);
        console.log('Status type:', typeof appointmentData[0].status);
    }
</script>

<script>
    // ========================================
    // STAFF CALENDAR MODULE
    // ========================================
    const DashboardCalendar = (() => {
        let currentDate = new Date();
        let selectedDate = new Date();
        let appointments = [];

        const getLocalDateString = (date) => {
            const year = date.getFullYear();
            const month = String(date.getMonth() + 1).padStart(2, '0');
            const day = String(date.getDate()).padStart(2, '0');
            return `${year}-${month}-${day}`;
        };

        const init = (appointmentData) => {
            appointments = appointmentData;
            renderCalendar();
            renderAppointments(getLocalDateString(selectedDate));

            document.getElementById('prevMonth')?.addEventListener('click', () => {
                currentDate.setMonth(currentDate.getMonth() - 1);
                renderCalendar();
            });

            document.getElementById('nextMonth')?.addEventListener('click', () => {
                currentDate.setMonth(currentDate.getMonth() + 1);
                renderCalendar();
            });
        };

        const renderCalendar = () => {
            const calendar = document.getElementById('calendar');
            const monthYear = document.getElementById('monthYear');
            if (!calendar || !monthYear) return;

            const year = currentDate.getFullYear();
            const month = currentDate.getMonth();

            monthYear.textContent = new Date(year, month).toLocaleDateString('en-US', {
                month: 'long',
                year: 'numeric'
            });

            const firstDay = new Date(year, month, 1).getDay();
            const daysInMonth = new Date(year, month + 1, 0).getDate();

            const todayStr = getLocalDateString(new Date());
            const selectedStr = getLocalDateString(selectedDate);

            let calendarHTML = '';

            for (let i = 0; i < firstDay; i++) {
                calendarHTML += '<div class="calendar-day empty"></div>';
            }

            for (let day = 1; day <= daysInMonth; day++) {
                const date = new Date(year, month, day);
                const dateStr = getLocalDateString(date);
                const dateAppointments = appointments.filter(apt => apt.date === dateStr);
                const hasAppointments = dateAppointments.length > 0;
                const allCompleted = hasAppointments && dateAppointments.every(apt => apt.status && apt.status.toLowerCase() === 'completed');

                const isToday = dateStr === todayStr;
                const isSelected = dateStr === selectedStr;

                let classes = 'calendar-day';
                if (isToday) classes += ' today';
                if (isSelected) classes += ' selected';
                if (hasAppointments) classes += ' has-appointments';
                if (allCompleted) classes += ' all-completed';

                calendarHTML += `
                    <div class="${classes}" onclick="selectDate('${dateStr}')">
                        <span class="day-number">${day}</span>
                        ${hasAppointments ? '<span class="appointment-dot"></span>' : ''}
                    </div>
                `;
            }

            calendar.innerHTML = calendarHTML;
        };

        const renderAppointments = (dateStr) => {
            const appointmentsList = document.getElementById('appointmentsList');
            const selectedDateBadge = document.getElementById('selectedDate');
            if (!appointmentsList) return;

            const dateAppointments = appointments.filter(apt => apt.date === dateStr);

            if (selectedDateBadge) {
                const date = new Date(dateStr + 'T00:00:00');
                const todayStr = getLocalDateString(new Date());

                if (dateStr === todayStr) {
                    selectedDateBadge.textContent = 'Today';
                } else {
                    selectedDateBadge.textContent = date.toLocaleDateString('en-US', {
                        month: 'short',
                        day: 'numeric'
                    });
                }
            }

            if (dateAppointments.length === 0) {
                appointmentsList.innerHTML = '<p class="no-appointments">No appointments scheduled</p>';
                return;
            }

            appointmentsList.innerHTML = dateAppointments.map(apt => `
                <div class="appointment-item ${apt.status}">
                    <div class="appointment-time">${apt.time}</div>
                    <div class="appointment-details">
                        <div class="appointment-pet">${apt.petName}</div>
                        <div class="appointment-service">${apt.serviceType}</div>
                        <div class="appointment-groomer">with ${apt.groomerName}</div>
                    </div>
                    <div class="appointment-status-wrapper">
                        <span class="appointment-status ${apt.status}">${capitalizeStatus(apt.status)}</span>
                        ${apt.status && apt.status.toLowerCase() === 'confirmed' ? `
                            <button class="btn-status-update" onclick="updateAppointmentStatus('${apt.id}', 'Completed', this)" title="Mark as completed">
                                ✓ Complete
                            </button>
                        ` : ''}
                    </div>
                </div>
            `).join('');
        };

        const selectDate = (dateStr) => {
            selectedDate = new Date(dateStr + 'T00:00:00');
            renderCalendar();
            renderAppointments(dateStr);
        };

        return { init, selectDate, renderAppointments, renderCalendar };
    })();

    const capitalizeStatus = (status) => {
        return status.charAt(0).toUpperCase() + status.slice(1);
    };

    window.selectDate = (dateStr) => DashboardCalendar.selectDate(dateStr);

    window.updateAppointmentStatus = async (appointmentId, newStatus, buttonElement) => {
        try {
            const button = buttonElement;
            button.disabled = true;
            const originalText = button.textContent;
            button.innerHTML = '<span class="loading-spinner"></span>';

            // Get CSRF token from the hidden input
            const csrfTokenElement = document.querySelector('input[name="__RequestVerificationToken"]');
            if (!csrfTokenElement) {
                alert('Error: CSRF token not found. Please refresh the page.');
                button.disabled = false;
                button.innerHTML = originalText;
                return;
            }

            const csrfToken = csrfTokenElement.value;

            const response = await fetch('@Url.Action("UpdateAppointmentStatus", "Home")', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'RequestVerificationToken': csrfToken
                },
                body: JSON.stringify({
                    appointmentId: appointmentId,
                    newStatus: newStatus
                })
            });

            console.log('Response status:', response.status);
            console.log('Response ok:', response.ok);

            // Parse response as JSON
            let data = {};
            try {
                const responseText = await response.text();
                console.log('Raw response:', responseText);
                data = JSON.parse(responseText);
            } catch (e) {
                console.error('Failed to parse response as JSON:', e);
                alert('Error: Invalid response from server. Please check the browser console for details.');
                button.disabled = false;
                button.innerHTML = originalText;
                return;
            }

            console.log('Response data:', data);

            if (!data.success) {
                alert('Error: ' + (data.message || 'Failed to update appointment.'));
                button.disabled = false;
                button.innerHTML = originalText;
                return;
            }

            // Success - show message
            alert(data.message || 'Appointment updated successfully');

            // Update the appointment in the local array
            const appointment = appointmentData.find(apt => apt.id === appointmentId);
            if (appointment) {
                appointment.status = data.newStatus || newStatus.toLowerCase();
            }

            // Get the currently selected date
            const today = new Date();
            const year = today.getFullYear();
            const month = String(today.getMonth() + 1).padStart(2, '0');
            const day = String(today.getDate()).padStart(2, '0');
            const currentDateStr = `${year}-${month}-${day}`;

            // Re-render the calendar and appointments using the public methods
            DashboardCalendar.renderCalendar();
            DashboardCalendar.renderAppointments(currentDateStr);
        } catch (error) {
            console.error('Error updating appointment:', error);
            alert('Error updating appointment: ' + error.message);
            const button = buttonElement;
            button.disabled = false;
            button.innerHTML = originalText;
        }
    };

    // ========================================
    // INITIALIZE CALENDAR
    // ========================================
    document.addEventListener('DOMContentLoaded', () => {
        DashboardCalendar.init(appointmentData);
    });
</script>